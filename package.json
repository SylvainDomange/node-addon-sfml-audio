{
  "name": "sfml-audio-node",
  "version": "1.0.1",
  "description": "sfml-audio binding with an added mp3 decoder",
  "private": true,
  "scripts": {
    "clean": "cmake-js clean",
    "config-debug": "cmake-js configure -DCMAKE_BUILD_TYPE=Debug",
    "config": "cmake-js configure",
    "build-debug": "cmake-js build -DCMAKE_BUILD_TYPE=Debug",
    "build": "cmake-js build",
    "rebuild-debug": "cmake-js rebuild -DCMAKE_BUILD_TYPE=Debug",
    "rebuild": "cmake-js rebuild",
    "compile-debug": "cmake-js compile -DCMAKE_BUILD_TYPE=Debug",
    "compile": "cmake-js compile",
    "dist": "cmake-js build --CD --target install",
    "start": "cd bin && node index.js"
  },
  "author": "Sylvain",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.14.20",
    "node-addon-api": "^3.1.0"
  }
}
